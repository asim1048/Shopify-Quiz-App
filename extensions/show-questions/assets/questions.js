// Select the submit button by its type
let submitButton = document.querySelector('input[type="submit"]');

submitButton.addEventListener("click", function () {
   alert("This is from choco app");
});
